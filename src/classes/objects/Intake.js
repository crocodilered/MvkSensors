import AbstactObject from 'classes/objects/AbstactObject'

class Intake extends AbstactObject {

  constructor (center, title) {
    super(center, title)
    this.classList = ['intake']
  }

}

 export default Intake
